<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"

    android:id="@+id/add_copy_dialog_id"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/dialog_header"
        android:text="Please complete the informations below"
        android:textSize="28sp"
        android:textAlignment="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <EditText
        android:layout_below="@+id/dialog_header"
        android:id="@+id/copy_price"
        android:layout_marginTop="30dp"
        android:hint="Enter the price here"
        android:inputType="numberDecimal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Spinner
        android:id="@+id/integrity_dropdown"
        android:layout_marginTop="30dp"
        android:layout_below="@id/copy_price"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    </Spinner>

    <Button
        android:layout_below="@id/integrity_dropdown"
        android:id="@+id/confirm_dialog_button"
        android:layout_marginTop="30dp"
        android:textStyle="normal"
        android:textAllCaps="false"
        android:text="Add Copy"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</RelativeLayout>